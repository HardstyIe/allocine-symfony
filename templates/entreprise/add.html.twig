{% extends 'base.html.twig' %}

{% block body %}
    <h2>Ajout d'une entreprise</h2>
{# Formulaire #}
    <form action="{{ path('app_entreprise_add_new') }}" method="POST">
        <input name="name" type="text" required/>
        <button class="btn btn-success">Enregistrer</button>
    </form>
{% endblock %}