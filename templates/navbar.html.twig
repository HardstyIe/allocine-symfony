<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_actor_index') }}">Acteurs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_movie_index') }}">Film</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_realisator_index') }}">Realisateur</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_category_index') }}">Category/Genre</a>
                </li>
                
            </ul>
        </div>
        <div>

            {% if app.user %}
                <a href="{{ path('app_profile') }}" class="btn btn-outline-primary">Mon profil</a>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Deconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-outline-primary">Connexion</a>
                <a href="{{ path('app_register') }}" class="btn btn-outline-success">Inscription</a>
            {% endif %}
        </div>
    </div>
</nav>
