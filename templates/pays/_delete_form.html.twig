<form method="post" action="{{ path('app_pays_delete', {'id': pays.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pays.id) }}">
    <button class="btn">Supprimer</button>
</form>
