{% extends 'base.html.twig' %}

{% block title %}Acteur{% endblock %}

{% block body %}
    <h1>Acteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ actor.firstName }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ actor.lastName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_actor_edit', {'id': actor.id}) }}">Modifiez</a>
    
    <a href="{{ path('app_actor_index') }}">Retour a la liste des Acteurs</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
